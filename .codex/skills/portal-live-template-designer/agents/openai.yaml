interface:
  display_name: "Portal Live Template Designer"
  short_description: "Build and maintain tenant live templates"
  default_prompt: "Use $portal-live-template-designer to update a tenant template safely and prepare it for deployment."
